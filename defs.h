#ifndef DEFS_H
#define DEFS_H

// Centralized list of parameters

enum charColor {BLUE, CYAN, GREEN, LBLUE, LGREEN, LPINK, MOSS, PINK, PURPLE}; //colors of the characters

const int SCENE_WIDTH = 1918;           // Scene dimensions
const int SCENE_HEIGHT = 975;

const int PLAYER_WIDTH = 112;           // Player dimensions
const int PLAYER_HEIGHT = 112;

const int TAGGER_WIDTH = 128;           // Tagger dimensions
const int TAGGER_HEIGHT = 128;

const int OBSTACLE_WIDTH = 30;          // Obstacle dimensions
const int OBSTACLE_HEIGHT = 30;

const int FRAME_RATE = 24;              // Time interval between frames in milliseconds

const int NUM_IMAGES = 36;              // Player Sprite Number
const int NUM_FRAMES = 9;

#endif // DEFS_H
